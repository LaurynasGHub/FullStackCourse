//enums yra budas priskirti pavadinima setui kuris talpina nekintancias reiksmes

//cia galima rasyti ir be = x, tada nuo 0 suzymes didejancia tvarka
enum Priority {
  Lowest = 0,
  Low = 1,
  Medium = 2,
  High = 3,
  Urgent = 4,
}

function addTicket(details: string, priority: Priority) {
  if (priority === Priority.Lowest) {
  }
}
